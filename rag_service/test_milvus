from pymilvus import connections, Collection

# 1. Kết nối tới Milvus (cổng gRPC mặc định 19530)
connections.connect(alias="default", host="localhost", port="19530")

# 2. Truy xuất collection
c = Collection("bank_mock")

# 3. In schema
print(c.schema)